" Add the g flag to search/replace by default
set gdefault

" Enable line numbers
set number

" Syntax highlighting
syntax on

" Highlight current line
set cursorline

" Tab size = 4 chars
set tabstop=4

" Show file title
set title

" Fix shitty backspace behaviour
set backspace=indent,eol,start

colorscheme desert
